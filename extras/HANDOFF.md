We implemented hooks for `sns_registry_insert_sorted` (offset 0xF88080) in `src/gamepatches/patches.cpp` to capture message type hash→name mappings and identify hash `0x59e4c5ea6e01083b`, but the hooks install too late—the function executes during Windows PE static initialization before `DllMain` runs, so despite hooks installing successfully (confirmed in test.log), they never fired because the function already completed. We created a Frida script at `scripts/dump_message_hashes.js` to hook earlier via spawn mode. Three options remain: use Frida spawn mode to hook before static initialization, binary patch `echovr.exe` to inject hooks before static init, or pivot to hooking `CMatSym_Hash` (which runs during gameplay, not startup) and manually compute final hashes—user must decide which path to take since current DLL injection approach is proven blocked.
