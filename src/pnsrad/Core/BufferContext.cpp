/* @module: pnsrad.dll */
/* @purpose: BufferContext operations — implementations moved to TLSMemory.cpp.
 *           This file is intentionally empty. All buffer_init, buffer_destroy,
 *           buffer_free_data, buffer_realloc, and buffer_grow implementations
 *           live alongside the TLS and memory utility functions they depend on. */
