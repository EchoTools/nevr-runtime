# standalone — Android/Quest standalone build of Echo VR
#
# Requires the echovr-reconstruction source to be complete enough to compile.
# Until then, this is a stub that registers the target but builds nothing.
#
# Target platform: Android ARM64 (Quest 2/3/Pro)

# TODO: Uncomment and flesh out once reconstruction reaches buildable state
# add_executable(echovr_standalone main.cpp)
# target_compile_features(echovr_standalone PRIVATE cxx_std_17)
